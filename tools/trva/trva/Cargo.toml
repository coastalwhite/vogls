[package]
name = "trva"
version = "0.1.0"
edition = "2024"

[dependencies]
foldhash = { version = "0.2.0", optional = true }
hashbrown = { version = "0.17.1", default-features = false }

[features]
default = ["foldhash"]
